import 'package:flutter/material.dart';

class FastLaugScreen extends StatelessWidget {
  const FastLaugScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(' fast laugh screen'),
      ),
    );
  }
}